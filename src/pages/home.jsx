import React from 'react'

import Properties from "../Components/property/properties"

const Home = () => {
  return (
    <div>
      {/* <div>
        {usersList.length > 0 ? (
          <Properties/>
        ) : (
          "No Properties found!"
        )}
      </div> */}
    </div>
  );
}

export default Home;
